## 2025-02-26 - [TOCTOU in Sensitive File Creation]
**Vulnerability:** Time-of-Check to Time-of-Use (TOCTOU) vulnerability where sensitive files (like `auth.json`) were created with default permissions using `std::fs::write` and then restricted using `std::fs::set_permissions(..., 0o600)`. This leaves a brief window where the file is readable by others.
**Learning:** Post-creation permission modification leaves a race condition window that can be exploited, especially for files storing API keys and credentials.
**Prevention:** Always use `std::fs::OpenOptions` with `std::os::unix::fs::OpenOptionsExt::mode(0o600)` to securely and atomically create the file with restricted permissions before writing any data to it.

## 2025-02-18 - Enforce Secure File Permissions via Atomic Writes for Configuration Files
**Vulnerability:** Configuration files containing sensitive data (like MCP OAuth client secrets or access tokens) were written using `std::fs::write` or non-atomic serialization. This creates a Time-of-Check to Time-of-Use (TOCTOU) race condition and defaults to standard user permissions, potentially allowing unauthorized read access on multi-user systems.
**Learning:** Directly modifying permissions after writing a file still leaves a short window where a local attacker can read or modify the file.
**Prevention:** Always write sensitive files using an atomic pattern: create a temporary file using `std::fs::OpenOptions` with `.create(true).write(true).truncate(true).mode(0o600)` (on Unix via `std::os::unix::fs::OpenOptionsExt`), write the contents, and then use `std::fs::rename` to atomically replace the destination file.

## 2025-05-14 - Fix TOCTOU vulnerability in Application State Snapshots
**Vulnerability:** Time-of-Check to Time-of-Use (TOCTOU) vulnerability where `AppStateSnapshot` data was written via `std::fs::write(&tmp_path, &json)` before renaming. This could allow unauthorized access to sensitive application state on multi-user systems since default permissions were used, and atomic writing alone does not secure the temp file itself during writing.
**Learning:** Even short-lived temporary files used for atomic renaming can be intercepted or read during the write process if they contain sensitive system state (like API keys or session information) and are created with default broad permissions.
**Prevention:** Always write sensitive state serialization using an atomic pattern with `.create_new(true)` and `.mode(0o600)` (on Unix) via `std::fs::OpenOptions` instead of `std::fs::write`, to securely enforce read/write restrictions before data touches the disk.

## 2025-05-16 - [Fix TOCTOU vulnerability in Configuration Atomic Writes]
**Vulnerability:** Across various configuration files (`auth.rs`, `user_store.rs`, `mcp/io.rs`, `config.rs`, `loader/mod.rs`), atomic writes utilized `.create(true).truncate(true)` instead of `.create_new(true)`. On non-Unix platforms, the fallback utilized `std::fs::write(&tmp_path, ...)`. This resulted in a Time-of-Check to Time-of-Use (TOCTOU) vulnerability where an attacker could theoretically predict the randomized temporary filename or exploit symlinks to intercept sensitive configuration material.
**Learning:** Even when employing randomized UUIDs for temporary file names within atomic write patterns, utilizing `create(true)` or `std::fs::write` leaves a theoretical attack surface for unauthorized reading and symlink attacks.
**Prevention:** Unconditionally employ `.create_new(true)` to securely enforce that temporary files are exclusively created afresh by the process, failing securely if the path already exists or has been externally manipulated. Apply this standard universally to both Unix and non-Unix write blocks when persisting sensitive settings or application state.

## 2025-05-17 - Path Traversal in state_snapshot.rs
**Vulnerability:** A path traversal vulnerability existed in `crates/opendev-runtime/src/state_snapshot.rs` where the `session_id` was directly substituted into the snapshot filename without sanitization. An attacker or bug could supply a malformed `session_id` containing `../` characters, resulting in files being written or read outside of the intended directory.
**Learning:** File paths dynamically constructed using unsanitized user inputs or unbounded strings without proper normalization can lead to arbitrary file reads or writes. Even internally generated IDs should be sanitized if they are part of a file path, especially if there's potential for external contamination or test data with non-standard formats.
**Prevention:** Always sanitize components of a file path derived from external or semi-external data by replacing or stripping path separator characters (like `/` and `\`) before constructing the final path.

## 2024-05-20 - [Fix TOCTOU vulnerability in Session Manager]
**Vulnerability:** Found `std::fs::write` used for writing temporary files during an atomic write sequence in `crates/opendev-history/src/session_manager/mod.rs` (for session JSON and JSONL files). This exposes a Time-Of-Check to Time-Of-Use (TOCTOU) vulnerability where a malicious actor could pre-create a symlink on Unix systems to overwrite arbitrary files when the application writes to the temporary path.
**Learning:** Atomic writes must securely create the temporary file rather than just blindly writing to a path. This app specifically relies on this pattern when managing files across sessions and state files.
**Prevention:** Always use `std::fs::OpenOptions` with `.write(true).create_new(true)` to ensure the temporary file is exclusively created. On Unix systems, also enforce restricted permissions via `.mode(0o600)` to ensure secure visibility.

## 2025-05-20 - [Fix XSS vulnerability in DocumentationViewer]
**Vulnerability:** A Cross-Site Scripting (XSS) vulnerability was identified in `web-ui/src/components/CodeWiki/DocumentationViewer.tsx` where user-provided markdown content was being manually parsed using string replacement and injected directly into the DOM using React's `dangerouslySetInnerHTML`. An attacker could exploit this by embedding malicious scripts or HTML payloads inside the markdown content, which would execute in the victim's browser.
**Learning:** Using `dangerouslySetInnerHTML` combined with rudimentary, custom string replacement (e.g., regex `.replace`) for markdown parsing is highly prone to bypasses and script injection.
**Prevention:** Always use an established, secure markdown parsing library (like `react-markdown`) that properly sanitizes and renders markdown content without manually manipulating raw HTML strings.

## 2025-05-21 - [Fix TOCTOU vulnerability in Session Index Atomic Write]
**Vulnerability:** A Time-Of-Check to Time-Of-Use (TOCTOU) vulnerability was found in `crates/opendev-history/src/index.rs` where the session index temporary file (`.sessions-index-tmp.json`) was written using `std::fs::write` with a predictable (deterministic) filename. An attacker could pre-create a symlink at this location, redirecting the write to overwrite arbitrary files, or exploit the lack of restricted permissions to read sensitive session metadata.
**Learning:** Atomic write patterns that use a predictable temporary filename and `std::fs::write` are vulnerable to symlink attacks and information disclosure if the file permissions are not explicitly restricted during creation.
**Prevention:** Always use randomized (UUID-based) temporary filenames combined with `std::fs::OpenOptions` implementing `.create_new(true)` to ensure exclusive file creation. On Unix systems, always apply `.mode(0o600)` to prevent unauthorized access before the file is atomically renamed.

## 2025-06-25 - [Fix TOCTOU vulnerability in Artifact Index saving]
**Vulnerability:** A Time-of-Check to Time-of-Use (TOCTOU) vulnerability existed in `crates/opendev-context/src/compaction/artifacts.rs` inside the `ArtifactIndex::save_to_file` function where temporary files were written with `std::fs::write(&tmp, &json)` before renaming. This creates a race condition where the temp file could be read or manipulated between writing and renaming.
**Learning:** `std::fs::write` does not safely guarantee creation of new files and uses standard permissive default attributes. When an application serializes and saves potentially sensitive or critical state objects using the write-to-temp-and-rename pattern, the temporary file must be secured as strictly as the final file.
**Prevention:** Unconditionally enforce strict read/write restrictions before data touches disk by using `std::fs::OpenOptions` with `.create_new(true)` and `.mode(0o600)` (on Unix platforms) along with randomized file names utilizing `uuid`.

## 2026-05-26 - [Fix TOCTOU vulnerability in Mailbox write]
**Vulnerability:** A Time-of-Check to Time-of-Use (TOCTOU) vulnerability existed in `crates/opendev-runtime/src/mailbox/mod.rs` where the `write_messages` function used `std::fs::write(&self.inbox_path, json)`. This writes data directly to a predictable path without restricted permissions or atomic replacement. Since file permissions default to broad access during the write window, sensitive mailbox communication between agents could be intercepted or manipulated.
**Learning:** Even when concurrent writes are serialized via an external file lock (e.g. `fd_lock` on a separate `.lock` file), the actual destination file must still be written using an atomic write pattern (`std::fs::OpenOptions::new().create_new(true).mode(0o600)` to a randomized `.tmp` file, followed by `std::fs::rename`) to prevent race conditions and enforce strict local system permissions while data touches disk.
**Prevention:** Unconditionally utilize `OpenOptions` with `.create_new(true)` and `.mode(0o600)` (on Unix platforms) along with randomized file names (e.g., using `uuid`) when saving or serializing application state, including messaging systems and queues.

## 2025-05-27 - [Fix TOCTOU vulnerability in file_checkpoint manifest saving]
**Vulnerability:** A Time-Of-Check to Time-Of-Use (TOCTOU) vulnerability was found in `crates/opendev-history/src/file_checkpoint.rs` where the file checkpoint manifest temporary file (`.manifest.[uuid].tmp`) was written using `std::fs::write`. An attacker could theoretically predict the randomized temporary filename or exploit symlinks to intercept sensitive configuration material.
**Learning:** Even when employing randomized UUIDs for temporary file names within atomic write patterns, utilizing `std::fs::write` leaves a theoretical attack surface for unauthorized reading and symlink attacks.
**Prevention:** Always use `std::fs::OpenOptions` with `.write(true).create_new(true)` to ensure the temporary file is exclusively created. On Unix systems, always apply `.mode(0o600)` to prevent unauthorized access before the file is atomically renamed.

## 2025-02-27 - [Fix TOCTOU vulnerability in Persistent Rules]
**Vulnerability:** A Time-Of-Check to Time-Of-Use (TOCTOU) vulnerability was found in `crates/opendev-runtime/src/approval/persistence.rs` where `permissions.json` was written using `std::fs::write`. This non-atomic operation allows local attackers to exploit race conditions, overwrite arbitrary files using symlinks, and defaults to permissive access limits leaving sensitive custom rules exposed.
**Learning:** `std::fs::write` cannot safely create files with strict access limitations from inception and makes atomic overwrite impossible, threatening sensitive operations involving global user configurations.
**Prevention:** Unconditionally adopt the atomic file write pattern utilizing `std::fs::OpenOptions::new().create_new(true).write(true)` coupled with randomized temporary file names based on `uuid::Uuid::new_v4()`. Enforce `.mode(0o600)` during creation on Unix systems to strictly govern read/write access prior to data ingestion, followed directly by an atomic `std::fs::rename`.

## 2025-07-01 - [Fix TOCTOU vulnerability in TUI history persistence]
**Vulnerability:** A Time-Of-Check to Time-Of-Use (TOCTOU) vulnerability was found in `crates/opendev-tui/src/history.rs` where the TUI command history was persisted directly to disk using `std::fs::write(&self.file_path, json)`. Because `std::fs::write` creates files with standard default permissions, sensitive data from terminal commands could be exposed to other users on a multi-user system, or the file could be targeted by a symlink attack.
**Learning:** Persisting any sensitive application state (like command history, auth info, or context) directly with `std::fs::write` fails securely restrict file permissions from the moment of creation, leaving a window for unintended access or modification.
**Prevention:** Always write sensitive artifacts securely using an atomic write pattern by exclusively creating a temporary file with randomized names via `std::fs::OpenOptions::new().write(true).create_new(true)`, and on Unix environments enforcing `.mode(0o600)`, before issuing an atomic `std::fs::rename`.

## 2025-02-27 - [TOCTOU in State Snapshot and Config Persistence]
**Vulnerability:** Similar to the previous discovery of TOCTOU vulnerabilities, `std::fs::write` was used to directly persist sensitive information like application state snapshots, team manager configs, team task lists, and plugin marketplaces. `std::fs::write` writes files with default permissions before any changes could be made, allowing an unauthorized local actor to read sensitive state and configuration files before permissions are adjusted, creating a Race Condition.
**Learning:** Persisting any application state containing sensitive configurations, task lists, and snapshots should utilize an atomic writing process. Post-creation permission modification leaves a short vulnerability window.
**Prevention:** Always write sensitive state files securely using `std::fs::OpenOptions` with `std::os::unix::fs::OpenOptionsExt::mode(0o600)` to securely create an atomic temporary file before writing data to it, and using `std::fs::rename` to replace the destination file.

## 2025-02-28 - [Fix TOCTOU vulnerability in fs_utils atomic_write_secure]
**Vulnerability:** Across various utilities using `atomic_write_secure` (specifically in `crates/opendev-runtime/src/fs_utils.rs` and `crates/opendev-plugins/src/fs_utils.rs`), atomic writes utilized `.create(true).truncate(true)` instead of `.create_new(true)`. On non-Unix platforms, the fallback utilized `std::fs::write(&tmp_path, ...)`. This resulted in a Time-of-Check to Time-of-Use (TOCTOU) vulnerability where an attacker could theoretically predict the randomized temporary filename or exploit symlinks to intercept sensitive configuration material.
**Learning:** Even when employing randomized UUIDs for temporary file names within atomic write patterns, utilizing `create(true)` or `std::fs::write` leaves a theoretical attack surface for unauthorized reading and symlink attacks.
**Prevention:** Always write sensitive files using an atomic pattern: create a temporary file using `std::fs::OpenOptions` with `.create_new(true).write(true)` (on Unix via `std::os::unix::fs::OpenOptionsExt::mode(0o600)`), write the contents, and then use `std::fs::rename` to atomically replace the destination file.

## 2026-06-03 - [Fix TOCTOU vulnerability in LSP utils FileUtils::atomic_write]
**Vulnerability:** Insecure temporary file creation in `crates/opendev-tools-lsp/src/utils.rs` `FileUtils::atomic_write`. The code used a deterministic temporary filename (e.g., `.file.tmp`) and `std::fs::write`, which doesn't enforce exclusive file creation or use secure permissions. A malicious actor could pre-create the temporary file as a symlink to an arbitrary system file, causing the application to overwrite sensitive data on atomic write and rename.
**Learning:** When performing atomic file writes, the temporary file must be created with a highly randomized name (UUID) and use `OpenOptions` with `.create_new(true)` to guarantee exclusive file creation. On Unix, setting `.mode(0o600)` ensures the written data is not readable by unauthorized users before the rename completes.
**Prevention:** Use `uuid::Uuid::new_v4()` to generate random temporary filenames. Use `std::fs::OpenOptions::new().write(true).create_new(true)` to safely create the temporary file, and use `.mode(0o600)` on Unix systems to restrict permissions. Avoid using `std::fs::write` or deterministic names like `.tmp` for temporary files in atomic write operations.

## 2025-06-07 - [Fix TOCTOU vulnerability in models_dev cache sync]
**Vulnerability:** A Time-Of-Check to Time-Of-Use (TOCTOU) vulnerability was found in `crates/opendev-config/src/models_dev/sync.rs` where the local provider configurations derived from the `models.dev` catalog were cached directly to disk using `std::fs::write(&path, json)`. Because `std::fs::write` creates files with non-restrictive default permissions and isn't atomic, a local attacker could employ a symlink attack or exploit the permissions window to intercept, read, or manipulate API endpoint mapping and default configuration values.
**Learning:** Config registries and local caching solutions storing external endpoint details, configuration flags, or structured catalog files must always employ an atomic write pattern with locked-down default system permissions when data begins writing to disk. Writing any serialized struct to disk using `std::fs::write` carries implicit permissions risks and lacks atomicity.
**Prevention:** Avoid writing application caches or configurations directly with `std::fs::write`. Use an atomic write pattern: generate a randomized temporary filename using `uuid::Uuid::new_v4()`, invoke `std::fs::OpenOptions::new().write(true).create_new(true)` (and explicitly mandate `.mode(0o600)` on Unix platforms via `OpenOptionsExt`), write the buffer, and complete the action via `std::fs::rename`.

## 2025-06-25 - [Fix CRITICAL path traversal/TOCTOU symlink attack in sharing.rs]
**Vulnerability:** A Time-Of-Check to Time-Of-Use (TOCTOU) symlink vulnerability existed in `crates/opendev-history/src/sharing.rs` where an anonymized session's HTML was written directly to the system temp directory (`std::env::temp_dir()`) using `std::fs::write(&path, html)` with a predictable filename (`opendev-session-{id}.html`). Since `/tmp` is world-writable, a local attacker could pre-create this file as a symlink pointing to an arbitrary sensitive file (like `/etc/passwd` or `~/.ssh/authorized_keys`). When the application tries to save the session, `std::fs::write` will follow the symlink and overwrite the target file with the HTML content.
**Learning:** Writing to world-writable directories (like `std::env::temp_dir()`) with predictable filenames and permissive functions like `std::fs::write` creates a critical vulnerability for symlink-based arbitrary file overwrite attacks.
**Prevention:** Always use `std::fs::OpenOptions` with `.write(true).create_new(true)` to ensure the temporary file is exclusively created and fails if a symlink or file already exists. On Unix systems, always apply `.mode(0o600)` to prevent unauthorized access before the file is written.

## 2025-07-25 - [Fix TOCTOU vulnerability in opendev-tools-impl temp files]
**Vulnerability:** A Time-Of-Check to Time-Of-Use (TOCTOU) symlink vulnerability was found in `crates/opendev-tools-impl` where predictable temporary filenames were generated and written to using `std::fs::write` in a world-writable directory (`std::env::temp_dir()`). This affected browser snapshots (`browser.rs`), ignore files (`excludes.rs`), and web screenshots (`web_screenshot.rs`). A local attacker could pre-create these files as symlinks to overwrite arbitrary system files when the tools run.
**Learning:** Writing any file to a shared, world-writable directory like `/tmp` requires extreme caution. Relying on `std::fs::write` with predictable filenames (like `browser_[pid].html` or `opendev-search-excludes.ignore`) completely bypasses exclusive creation checks, exposing the application to symlink attacks.
**Prevention:** For temporary files in shared directories, always generate a random filename (e.g., using `uuid::Uuid::new_v4()`) and write using `std::fs::OpenOptions::new().write(true).create_new(true)` to ensure exclusive creation. On Unix systems, additionally restrict permissions using `mode(0o600)` to prevent unauthorized reading or modification.

## 2025-02-15 - [TOCTOU Vulnerability in Crash Reporter]
**Vulnerability:** The crash reporter in `crates/opendev-cli/src/helpers.rs` uses `std::fs::write` with a predictable timestamp-based filename inside the global `~/.opendev/crash/` directory. If an attacker can guess the timestamp or symlink the directory, they could overwrite arbitrary files with the crash report due to a Time-of-Check-to-Time-of-Use (TOCTOU) vulnerability.
**Learning:** Even simple utility functions like crash reporters or logger trunctions handling files in user directories need secure file writing mechanisms. Deterministic filenames and `std::fs::write` are unsafe for sensitive operations.
**Prevention:** Always use `std::fs::OpenOptions` with `.create_new(true)` and randomized/UUID filenames, along with restricted file permissions (e.g., `0o600` on Unix), when generating files in global/user directories.

## 2025-09-08 - [Fix TOCTOU vulnerability in file write tool]
**Vulnerability:** A Time-Of-Check to Time-Of-Use (TOCTOU) vulnerability was found in `crates/opendev-tools-impl/src/file_write.rs` where file writes used `std::fs::write(&tmp_path, content)` during the atomic write pattern. Because `std::fs::write` creates temporary files with non-restrictive default permissions and lacks `create_new(true)`, an attacker could pre-create a symlink or intercept sensitive content being written by the application to the file path before the atomic rename took place.
**Learning:** Tools designed to write arbitrary file contents on behalf of the user must utilize completely secure atomic write patterns. Falling back to `std::fs::write` to populate the temporary file compromises the operation because it does not enforce exclusive file creation (`O_EXCL` in UNIX) and fails to lock down permissions (`0o600`).
**Prevention:** Avoid writing to temporary files in an atomic write pattern directly with `std::fs::write`. Use `uuid::Uuid::new_v4()` to generate a random temporary filename, then use `std::fs::OpenOptions::new().write(true).create_new(true)` to ensure exclusive file creation. On Unix systems, restrict permissions using `mode(0o600)` via `std::os::unix::fs::OpenOptionsExt` before issuing an atomic `std::fs::rename`.

## 2025-09-08 - [Reverted overly strict permissions on file write tool]
**Learning:** Enforcing restrictive `.mode(0o600)` on temporary files created during atomic writes in general-purpose file modification tools (like `file_write.rs`) is dangerous. Since the permissions carry over upon rename, it prevents other system users, web servers, or container volumes from reading codebase files, causing widespread access regressions.
**Prevention:** Do not enforce `.mode(0o600)` during atomic writes unless the target file explicitly stores sensitive application state, secrets, or localized caching data. For generic codebase file updates, rely on the `umask` defaults (e.g., standard `std::fs::write` behavior) or preserve the existing file's permissions, while retaining `create_new(true)` with randomized names to provide defense-in-depth against TOCTOU.

## 2026-06-17 - Fix TOCTOU vulnerability in memory consolidation
**Vulnerability:** Using `std::fs::write` for creating a lock file (`.consolidation.lock`) is vulnerable to Time-of-Check to Time-of-Use (TOCTOU) symlink attacks. It doesn't guarantee exclusive creation, and could be exploited if an attacker symlinks the lock file to a sensitive location.
**Learning:** The standard library's `std::fs::write` internally truncates and writes, which is unsafe for lock files. In a multi-user or concurrent environment, this can lead to privilege escalation or data corruption if symlinked to a different file.
**Prevention:** Always use `std::fs::OpenOptions` with `.create_new(true)` when creating lock files. This ensures the file is created atomically and fails if it already exists or if it's a symlink to an existing file.

## 2025-10-23 - [Fix CRITICAL TOCTOU vulnerability in telegram remote claim]
**Vulnerability:** A Time-Of-Check to Time-Of-Use (TOCTOU) vulnerability was found in `crates/opendev-channels/src/telegram/remote_claim.rs` where the session owner process ID (PID) file was written directly to the system temp directory (`std::env::temp_dir()`) using `fs::write(&pid_file, pid.to_string())`. Since the temp directory is often world-writable, a local attacker could pre-create this PID file as a symlink pointing to an arbitrary sensitive file. When the application tries to claim the session and save the PID, `fs::write` will follow the symlink and overwrite the target file with the PID content.
**Learning:** Writing to shared or world-writable directories (like `/tmp`) with predictable filenames and permissive functions like `fs::write` creates a critical vulnerability for symlink-based arbitrary file overwrite attacks. This also applies in test environments when simulating these writes.
**Prevention:** Always use `std::fs::OpenOptions` with `.write(true).create_new(true)` to ensure the temporary file is exclusively created, effectively defeating symlink attacks. On Unix systems, always apply `.mode(0o600)` to prevent unauthorized access before the file is atomically renamed to its final destination.

## 2026-06-25 - [Fix TOCTOU vulnerability in skills cache write]
**Vulnerability:** A Time-Of-Check to Time-Of-Use (TOCTOU) vulnerability existed in `crates/opendev-repl/src/skills.rs` where the `load_skill_from_url_with_options` function used `std::fs::write(&cache_path, &content)?` to write downloaded skills directly to the file system. Because `std::fs::write` creates temporary files with non-restrictive default permissions and lacks `create_new(true)`, an attacker could pre-create a symlink or intercept sensitive content being written by the application to the file path.
**Learning:** Config registries and local caching solutions storing external endpoint details, configuration flags, or structured catalog files (like downloaded skills) must always employ an atomic write pattern with locked-down default system permissions when data begins writing to disk.
**Prevention:** Avoid writing application caches or configurations directly with `std::fs::write`. Use an atomic write pattern: generate a randomized temporary filename using `uuid::Uuid::new_v4()`, invoke `std::fs::OpenOptions::new().write(true).create_new(true)` (and explicitly mandate `.mode(0o600)` on Unix platforms via `OpenOptionsExt`), write the buffer, and complete the action via `std::fs::rename`.

## 2025-06-25 - [Fix CRITICAL TOCTOU vulnerability in TUI background tasks]
**Vulnerability:** A Time-Of-Check to Time-Of-Use (TOCTOU) vulnerability was found in `crates/opendev-tui/src/managers/background_tasks.rs` where the initial output of background tasks was written directly to a somewhat predictable file in `/tmp/opendev/...` using `std::fs::write`. Since `/tmp` is typically world-writable, a local attacker could pre-create this file as a symlink pointing to an arbitrary sensitive file. When the application tries to save the output, `std::fs::write` will follow the symlink and overwrite the target file.
**Learning:** Writing to shared or world-writable directories (like `/tmp`) with predictable or short-entropy filenames and permissive functions like `std::fs::write` creates a critical vulnerability for symlink-based arbitrary file overwrite attacks.
**Prevention:** Always use `std::fs::OpenOptions` with `.write(true).create_new(true)` to ensure the temporary file is exclusively created, effectively defeating symlink attacks. On Unix systems, always apply `.mode(0o600)` to prevent unauthorized access before the file is populated.

## 2025-06-23 - [Fix TOCTOU vulnerability in memory consolidation files]
**Vulnerability:** Files written with `std::fs::write` on temporary files or direct paths in `memory_consolidation.rs` (like the consolidated output and meta json) can introduce symlink vulnerabilities because `std::fs::write` uses `.create(true).truncate(true)`, allowing an attacker to place a symlink to an arbitrary file beforehand.
**Learning:** Using `std::fs::write` is generally unsafe for cache files or state files due to Time-of-Check-to-Time-of-Use (TOCTOU) issues when not using proper permissions or locking, or atomic renames with random files.
**Prevention:** Use `OpenOptions` with `.create_new(true)` to create unique files atomically before renaming them over the target, ensuring no symlink follows.
## 2025-09-08 - [Fix TOCTOU vulnerability in file write tool]
**Vulnerability:** Multiple instances of `std::fs::write` were used with randomized filenames inside `/tmp` or system temporary directories for tools executing atomic writes (`insert_symbol.rs`, `multi_edit.rs`, `file_edit.rs`, `memory.rs`, and `session_memory.rs`). Although the filenames were randomized, `std::fs::write` alone does not strictly enforce exclusive file creation (`O_EXCL`), which still carries a marginal risk of a TOCTOU symlink attack if the directory is world-writable and an attacker manages to predict the UUID or pre-populate common namespaces.
**Learning:** For atomic writes to be completely secure, any write operation—even those utilizing random UUIDs—must use `std::fs::OpenOptions` with `.write(true).create_new(true)`. This guarantees exclusive file creation and fails immediately if the target (or a symlink pointing to the target) already exists, entirely circumventing TOCTOU issues.
**Prevention:** Always replace `std::fs::write` with `std::fs::OpenOptions` combined with `.create_new(true)` when populating temporary files as part of an atomic rename pattern.

## 2025-10-30 - [Fix TOCTOU vulnerability in Memory and Tools file writing]
**Vulnerability:** A Time-Of-Check to Time-Of-Use (TOCTOU) vulnerability was found across multiple files (`session_memory.rs`, `memory.rs`, `insert_symbol.rs`, `multi_edit.rs`, `file_edit.rs`, `patch/structured.rs`, `patch/unified.rs`, `truncation.rs`, `schedule.rs`, `notebook_edit.rs`, `rename.rs`, `sanitizer.rs`, `loader/mod.rs`) where `std::fs::write` was used to create files or temporary files. Because `std::fs::write` writes files with permissive default attributes and is vulnerable to symlink attacks, a local attacker could exploit the race condition to read sensitive application state or overwrite files.
**Learning:** Config registries, local caching solutions, session states, and tools designed to write arbitrary file contents on behalf of the user must utilize completely secure atomic write patterns with `OpenOptions`. Using `std::fs::write` lacks exclusive file creation (`O_EXCL` in UNIX) and fails to lock down permissions (`0o600`).
**Prevention:** Avoid writing to files directly with `std::fs::write`. Use `uuid::Uuid::new_v4()` to generate a random temporary filename, then use `std::fs::OpenOptions::new().write(true).create_new(true)` to ensure exclusive file creation. On Unix systems, restrict permissions using `mode(0o600)` via `std::os::unix::fs::OpenOptionsExt` before issuing an atomic `std::fs::rename`.
## 2025-10-31 - [Fix TOCTOU vulnerability in `std::fs::File::create`]
**Vulnerability:** A Time-Of-Check to Time-Of-Use (TOCTOU) vulnerability was found across multiple files (`plan_index.rs`, `sync.rs`, `context_picker.rs`, `recorder.rs`) where `std::fs::File::create` was used to create files or temporary files. Because `std::fs::File::create` uses `.create(true).truncate(true)` instead of `.create_new(true)`, it does not guarantee exclusive file creation. An attacker could pre-create a symlink to an arbitrary file, and `File::create` would follow the symlink and truncate the target file, leading to arbitrary file overwrite.
**Learning:** `std::fs::File::create` is fundamentally unsafe for creating temporary files, lock files, or any files in world-writable directories because it is vulnerable to symlink attacks. It should be avoided in favor of `std::fs::OpenOptions` with `.write(true).create_new(true)`.
**Prevention:** Replace `std::fs::File::create` with `std::fs::OpenOptions::new().write(true).create_new(true)` (and `.mode(0o600)` on Unix when appropriate) to ensure exclusive file creation and prevent symlink attacks. For atomic writes, always use a randomized filename combined with `.create_new(true)`.
## 2025-10-31 - [Fix TOCTOU vulnerability in ToolResultSanitizer overflow file saving]
**Vulnerability:** A Time-Of-Check to Time-Of-Use (TOCTOU) vulnerability was found in `crates/opendev-tools-core/src/sanitizer.rs` where `std::fs::write` was used to create files containing potentially sensitive output from tool execution that exceeded the allowed maximum characters. Because `std::fs::write` writes files with permissive default attributes and is vulnerable to symlink attacks, a local attacker could exploit the race condition to read sensitive application state or overwrite files.
**Learning:** Tools designed to write arbitrary file contents on behalf of the user must utilize completely secure atomic write patterns with `OpenOptions`. Using `std::fs::write` lacks exclusive file creation (`O_EXCL` in UNIX) and fails to lock down permissions (`0o600`).
**Prevention:** Avoid writing to files directly with `std::fs::write`. Use `uuid::Uuid::new_v4()` to generate a random temporary filename, then use `std::fs::OpenOptions::new().write(true).create_new(true)` to ensure exclusive file creation. On Unix systems, restrict permissions using `mode(0o600)` via `std::os::unix::fs::OpenOptionsExt` before issuing an atomic `std::fs::rename`.
## 2026-10-31 - [Fix TOCTOU vulnerability in configuration write-back]
**Vulnerability:** A Time-Of-Check to Time-Of-Use (TOCTOU) vulnerability was identified in `crates/opendev-config/src/loader/mod.rs` where `std::fs::write` was used to write the updated configuration to disk. Because `std::fs::write` does not guarantee exclusive creation and uses permissive defaults, an attacker could pre-create a symlink to perform an arbitrary file overwrite.
**Learning:** Even "best-effort" configuration cache writes or background updates can expose the application to TOCTOU symlink vulnerabilities if they are written using `std::fs::write`.
**Prevention:** Replace instances of `std::fs::write` with an atomic write pattern utilizing a randomized UUID temporary filename, `std::fs::OpenOptions` with `.create_new(true)`, and `.mode(0o600)` on Unix to ensure secure exclusive file creation.
## 2025-10-31 - [Fix TOCTOU vulnerability in session HTML sharing]
**Vulnerability:** A Time-Of-Check to Time-Of-Use (TOCTOU) vulnerability was found in `crates/opendev-history/src/sharing.rs` where an HTML file for sharing a session was created in `std::env::temp_dir()` using a predictable filename based on the session ID. Because `std::env::temp_dir()` (e.g. `/tmp`) is generally world-writable, a local attacker could pre-create a symlink using the predictable filename pointing to an arbitrary sensitive file. When `fs::OpenOptions` with `.create_new(true)` is used on some environments, it may not prevent symlink following completely if the filename is fully guessable. This could lead to arbitrary file overwriting.
**Learning:** Writing to shared temporary directories using predictable filenames is a critical vulnerability for symlink-based arbitrary file overwrite attacks.
**Prevention:** Always generate unpredictable filenames (e.g., using `uuid::Uuid::new_v4()`) when writing files to world-writable locations like `std::env::temp_dir()`. This must be combined with `std::fs::OpenOptions::new().write(true).create_new(true)` and, on Unix, `.mode(0o600)` to ensure exclusive file creation and prevent TOCTOU symlink attacks.

## 2025-10-31 - [Fix TOCTOU vulnerability in tool output truncation saving]
**Vulnerability:** A Time-Of-Check to Time-Of-Use (TOCTOU) vulnerability was found in `crates/opendev-tools-impl/src/truncation.rs` where `std::fs::write` was used to save truncated tool output to disk. Even though the filename included a random UUID, `std::fs::write` writes files with permissive default attributes and does not enforce exclusive creation, making it vulnerable to symlink-based arbitrary file overwrite attacks.
**Learning:** Tools designed to write large outputs to temporary locations on disk must use atomic write patterns with `OpenOptions`. Using `std::fs::write` is insecure because it lacks exclusive file creation (`O_EXCL` on UNIX) and fails to lock down permissions (e.g., `0o600`).
**Prevention:** Avoid writing files directly with `std::fs::write`. Generate a randomized temporary filename (e.g., using `uuid::Uuid::new_v4()`) and use `std::fs::OpenOptions::new().write(true).create_new(true)` to ensure exclusive file creation. On Unix systems, restrict permissions using `.mode(0o600)` via `std::os::unix::fs::OpenOptionsExt`.
## 2025-10-31 - [Fix TOCTOU vulnerability in remote skill caching]
**Vulnerability:** A Time-Of-Check to Time-Of-Use (TOCTOU) vulnerability was identified in `crates/opendev-agents/src/skills/discovery.rs` where `std::fs::write` was used to save fetched skill files into a cache directory. Because `std::fs::write` doesn't enforce exclusive creation, an attacker could pre-create a symlink and overwrite an arbitrary sensitive file when the application attempted to write the cached skill.
**Learning:** File caching mechanisms that save external content to disk (even in non-shared cache directories) are vulnerable to TOCTOU symlink attacks if they use non-exclusive write functions like `std::fs::write`.
**Prevention:** Always use atomic writes with `std::fs::OpenOptions` combined with `.create_new(true)` and a randomized UUID temporary filename. On Unix systems, ensure strict permissions (e.g., `0o600`) via `OpenOptionsExt` before renaming over the target path.
## 2025-10-31 - [Fix TOCTOU vulnerability in schedule.rs]
**Vulnerability:** Used `std::fs::write` to persist scheduled tasks in `crates/opendev-tools-impl/src/schedule.rs`. This function uses permissive default attributes and does not enforce exclusive file creation, making it vulnerable to symlink-based arbitrary file overwrite attacks.
**Learning:** For atomic writes to be entirely secure and robust against system crashes, `file.sync_all()` must be called before the file is closed and renamed. Furthermore, temporary files must be manually deleted if the write operation fails to avoid resource leaks. Using a dependency like `uuid` requires ensuring the `v4` feature flag is enabled in `Cargo.toml`.
**Prevention:** Replaced `std::fs::write` with an atomic write pattern utilizing a randomized UUID temporary filename, `std::fs::OpenOptions` with `.create_new(true)`, a `file.sync_all()` call, and error handling to remove the temp file on failure.
## 2025-10-31 - [Fix TOCTOU vulnerability in unified.rs]
**Vulnerability:** Used `std::fs::write` to persist patch content to disk in `crates/opendev-tools-impl/src/patch/unified.rs`. This function uses permissive default attributes and does not enforce exclusive file creation, making it vulnerable to symlink-based arbitrary file overwrite attacks.
**Learning:** For atomic writes to be entirely secure and robust against system crashes, `file.sync_all()` must be called before the file is closed and renamed. Furthermore, temporary files must be manually deleted if the write operation fails to avoid resource leaks. Using a dependency like `uuid` requires ensuring the `v4` feature flag is enabled in `Cargo.toml`.
**Prevention:** Replaced `std::fs::write` with an atomic write pattern utilizing a randomized UUID temporary filename, `std::fs::OpenOptions` with `.create_new(true)`, a `file.sync_all()` call, and error handling to remove the temp file on failure.

## 2025-10-31 - Fix TOCTOU vulnerability in session project marker
**Vulnerability:** A Time-Of-Check to Time-Of-Use (TOCTOU) vulnerability was found in `crates/opendev-history/src/session_manager/mod.rs` where `std::fs::write` was used to write the `OPENDEV_PROJECT_PATH` marker file. Because `std::fs::write` writes files with permissive default attributes and is vulnerable to symlink attacks, a local attacker could exploit the race condition to read sensitive application state or overwrite files.
**Learning:** Marker files recording directory state must utilize completely secure atomic write patterns with `OpenOptions` instead of `std::fs::write`.
**Prevention:** Avoid writing to files directly with `std::fs::write`. Use `uuid::Uuid::new_v4()` to generate a random temporary filename, then use `std::fs::OpenOptions::new().write(true).create_new(true)` to ensure exclusive file creation, sync data, and issue an atomic `std::fs::rename`.

## 2025-10-31 - Fix TOCTOU vulnerability in snapshot project marker
**Vulnerability:** A Time-Of-Check to Time-Of-Use (TOCTOU) vulnerability was found in `crates/opendev-history/src/snapshot.rs` where `std::fs::write` was used to write the `OPENDEV_PROJECT_PATH` marker file. Because `std::fs::write` writes files with permissive default attributes and is vulnerable to symlink attacks, a local attacker could exploit the race condition to read sensitive application state or overwrite files.
**Learning:** Marker files recording directory state must utilize completely secure atomic write patterns with `OpenOptions` instead of `std::fs::write`.
**Prevention:** Avoid writing to files directly with `std::fs::write`. Use `uuid::Uuid::new_v4()` to generate a random temporary filename, then use `std::fs::OpenOptions::new().write(true).create_new(true)` to ensure exclusive file creation, sync data, and issue an atomic `std::fs::rename`.
